// src/theme.ts
import { theme } from 'antd';

const customTheme = {
    token: {
        colorPrimary: '#4CAF50', // Customize your primary color
        colorLink: '#1DA57A', // Link color
        colorSuccess: '#52c41a', // Success color
        colorWarning: '#faad14', // Warning color
        colorError: '#f5222a', // Error color
        colorTextHeader: '#fff', // Text color for Header
        // Add more tokens as needed
    },
};

export default customTheme;
